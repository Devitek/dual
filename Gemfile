source "https://rubygems.org"

# Épinglé (pessimistic ~>) + Gemfile.lock commité : la release NE doit PAS
# dépendre de « la dernière version de fastlane du jour ». Mise à jour via PR
# dédiée (bundle update fastlane) — jamais implicitement dans le workflow.
gem "fastlane", "~> 2.239"
