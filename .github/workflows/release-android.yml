name: Release (Play Store)

# Build d'un AAB SIGNÉ (upload key), attaché à la GitHub Release, + upload Play
# optionnel. 100% gratuit (build GitHub Actions, pas d'EAS).
#
# Deux modes d'appel :
#   - workflow_call  : CHAÎNÉ depuis release-please (auto au merge de la PR de
#                      release). C'est le chemin nominal.
#   - workflow_dispatch : MANUEL (rebuild ponctuel / upload à la demande).
#
# versionCode = nombre de commits `git rev-list --count HEAD` : MONOTONE et
#   INDÉPENDANT du workflow qui build (évite tout rejet Play « versionCode doit
#   être supérieur »). Nécessite `fetch-depth: 0`.
# versionName = tag sans le « v » (passé en entrée, sinon dernière release).
#
# Secrets requis : ANDROID_KEYSTORE_BASE64, ANDROID_KEYSTORE_PASSWORD,
# ANDROID_KEY_ALIAS, ANDROID_KEY_PASSWORD (+ PLAY_SERVICE_ACCOUNT_JSON si upload).

on:
  workflow_call:
    inputs:
      tag:
        description: "Tag de la release à cibler (ex: v1.3.0). Vide = dernière release."
        type: string
        default: ""
      upload_to_play:
        description: "Envoyer aussi sur Google Play"
        type: boolean
        default: false
      track:
        description: "Piste Play (si upload)"
        type: string
        default: internal
      release_status:
        description: "Statut de la release Play (si upload)"
        type: string
        default: draft
  workflow_dispatch:
    inputs:
      upload_to_play:
        description: "Envoyer aussi sur Google Play"
        type: boolean
        default: false
      track:
        description: "Piste Play (si upload)"
        type: choice
        options: [internal, alpha, beta, production]
        default: internal
      release_status:
        description: "Statut de la release Play (si upload)"
        type: choice
        options: [draft, completed]
        default: draft

concurrency:
  group: release-android
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  release:
    name: Build signed AAB (+ optional Play upload)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          # Historique complet requis pour `git rev-list --count HEAD` (versionCode).
          fetch-depth: 0

      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
          bundler-cache: true

      - name: Install JS dependencies
        run: npm ci

      - name: Resolve version (name from tag, code from commit count)
        id: ver
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ inputs.tag }}"
          if [ -z "$TAG" ]; then
            TAG="$(gh release view --json tagName -q .tagName 2>/dev/null || true)"
          fi
          NAME="$(echo "$TAG" | sed 's/^v//')"
          CODE="$(git rev-list --count HEAD)"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "name=${NAME:-1.0.0}" >> "$GITHUB_OUTPUT"
          echo "code=${CODE}" >> "$GITHUB_OUTPUT"
          echo "Resolved versionName=${NAME:-1.0.0} versionCode=${CODE} (tag=${TAG:-none})"

      - name: Expo prebuild (Android)
        run: npx expo prebuild --platform android --no-install

      - name: Decode upload keystore
        run: echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > "$RUNNER_TEMP/upload.keystore"

      - name: Build signed AAB
        working-directory: android
        run: |
          ./gradlew bundleRelease --no-daemon \
            -Pandroid.injected.version.code=${{ steps.ver.outputs.code }} \
            -Pandroid.injected.version.name=${{ steps.ver.outputs.name }} \
            -Pandroid.injected.signing.store.file="$RUNNER_TEMP/upload.keystore" \
            -Pandroid.injected.signing.store.password="${{ secrets.ANDROID_KEYSTORE_PASSWORD }}" \
            -Pandroid.injected.signing.key.alias="${{ secrets.ANDROID_KEY_ALIAS }}" \
            -Pandroid.injected.signing.key.password="${{ secrets.ANDROID_KEY_PASSWORD }}"

      - name: Name the AAB
        id: aab
        run: |
          SRC=android/app/build/outputs/bundle/release/app-release.aab
          VER="${{ steps.ver.outputs.tag }}"
          [ -z "$VER" ] && VER="r${{ steps.ver.outputs.code }}"
          OUT="TwinLens-${VER}.aab"
          cp "$SRC" "$OUT"
          echo "path=$OUT" >> "$GITHUB_OUTPUT"

      - name: Attach AAB to the GitHub release
        if: steps.ver.outputs.tag != ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload "${{ steps.ver.outputs.tag }}" "${{ steps.aab.outputs.path }}" --clobber

      - name: Upload AAB artifact
        uses: actions/upload-artifact@v7
        with:
          name: twinlens-aab
          path: ${{ steps.aab.outputs.path }}
          if-no-files-found: error

      - name: Write Play service account key
        if: ${{ inputs.upload_to_play }}
        run: printf '%s' '${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}' > "$RUNNER_TEMP/play.json"

      - name: Upload to Google Play (Fastlane supply)
        if: ${{ inputs.upload_to_play }}
        env:
          AAB_PATH: ${{ steps.aab.outputs.path }}
          PLAY_SERVICE_ACCOUNT_JSON_PATH: ${{ runner.temp }}/play.json
          SUPPLY_TRACK: ${{ inputs.track }}
          SUPPLY_RELEASE_STATUS: ${{ inputs.release_status }}
        run: bundle exec fastlane android internal
